name: My First Workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps: 
        - name: checkout repo
          uses: actions/checkout@v4 

        - name: Welcome Message
          run: echo "Myfirst github actions job"

        - name: List Files
          run: ls

        - name: read File
          run: cat README.md